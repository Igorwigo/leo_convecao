<section class="py-20 bg-white" id="inscricao">
  <div class="container mx-auto px-4">
    <div class="max-w-2xl mx-auto">
      <h2 class="text-4xl font-bold text-center mb-8">Inscreva-se</h2>

      <%= turbo_frame_tag "registration_form" do %>
        <%= render "registrations/form", registration: Registration.new %>
      <% end %>
    </div>
  </div>
</section>
